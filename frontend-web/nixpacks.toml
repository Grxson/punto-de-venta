# Nixpacks config for Railway# Nixpacks configuration for Railway - Frontend Web (Vite + React)

# Use Node.js 22 (Vite requires 20.19+ or 22.12+)# This file configures Railway to build and deploy the Vite frontend



[phases.setup][phases.setup]

nixPkgs = ["nodejs_22", "npm"]nixPkgs = ["nodejs_20"]



[phases.install][phases.install]

cmds = ["npm ci"]cmds = ["npm ci"]



[phases.build][phases.build]

cmds = ["npm run build"]cmds = ["npm run build"]



[start][start]

cmd = "npx serve -s dist -l $PORT"cmd = "npx vite preview --host 0.0.0.0 --port $PORT"

