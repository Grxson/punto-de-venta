/**
 * Safe re-export of Recharts components
 * This module defers recharts imports to prevent circular dependency issues
 */

// Direct re-export - Vite will handle the module boundaries correctly
export * from 'recharts';
